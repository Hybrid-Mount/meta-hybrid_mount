{
  "name": "hybrid-mount-webui",
  "private": true,
  "version": "1.0.1",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "generate:constants": "node ./scripts/generate-constants.mjs",
    "generate:daemon-protocol": "node ./scripts/generate-daemon-protocol.mjs",
    "generate:info-tab-data": "node ./scripts/generate-info-tab-data.mjs",
    "generate": "pnpm generate:constants && pnpm generate:daemon-protocol && pnpm generate:info-tab-data",
    "prebuild": "pnpm generate",
    "dev": "pnpm generate:constants && pnpm generate:info-tab-data -- --prefer-existing && vite",
    "build": "vite build --emptyOutDir",
    "preview": "vite preview",
    "lint": "pnpm generate:constants && pnpm generate:info-tab-data -- --prefer-existing && pnpm generate:daemon-protocol -- --check && eslint . && tsc --noEmit",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "test": "pnpm generate:constants && pnpm generate:info-tab-data -- --prefer-existing && vitest run --environment node",
    "test:watch": "pnpm generate:constants && pnpm generate:info-tab-data -- --prefer-existing && vitest --environment node"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/jest-dom": "^6.9.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0",
    "vite": "^8.1.3",
    "vite-plugin-solid": "^2.11.12",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@material/web": "^2.4.1",
    "kernelsu": "^3.0.2",
    "solid-js": "^1.9.14",
    "zod": "^4.4.3"
  }
}
