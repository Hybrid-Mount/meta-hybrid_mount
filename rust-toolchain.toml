# SPDX-License-Identifier: GPL-3.0-only

[toolchain]
channel = "nightly"
# rust-src provides the std sources riscv64 needs, since no prebuilt std exists for it.
components = ["rustfmt", "clippy", "rust-src"]
profile = "minimal"
