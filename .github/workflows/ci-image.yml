# Copyright (C) 2026 YuzakiKokuban <heibanbaize@gmail.com>
#
# SPDX-License-Identifier: GPL-3.0-only

name: Build CI Image

on:
  push:
    branches:
      - 'dev'
    paths:
      - '.github/docker/ci/Dockerfile'
      - '.github/workflows/ci-image.yml'
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 1'

permissions:
  contents: read
  packages: write

env:
  CI_IMAGE: ghcr.io/hybrid-mount/meta-hybrid_mount-ci

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          filter: 'tree:0'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push CI image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: .github/docker/ci/Dockerfile
          push: true
          tags: |
            ${{ env.CI_IMAGE }}:latest
            ${{ env.CI_IMAGE }}:${{ github.sha }}
          cache-from: type=gha,scope=hybrid-mount-ci-image
          cache-to: type=gha,scope=hybrid-mount-ci-image,mode=max
